const { formatShopData, insertShops } = require("./shops-utils");
const {
  formatTreasureData,
  prepareTreasureData,
  insertTreasures,
} = require("./treasures-utils");

module.exports = {
  formatShopData,
  insertShops,
  formatTreasureData,
  prepareTreasureData,
  insertTreasures,
};
